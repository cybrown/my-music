{
  "name": "music",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && bower install && cd front && tsd install",
    "front-watch-typescript": "tsc -p front --watch ",
    "front-typescript": "tsc -p front",
    "front-watch-browserify": "watchify tmp/front/js/application.js -o front/public/app.js",
    "front-browserify": "browserify -e tmp/front/js/application.js -o front/public/app.js",
    "front-build": "npm run front-clean && npm run front-typescript && npm run front-browserify",
    "front-clean": "rimraf front/public/app.js && rimraf tmp/front",
    "front-serve": "http-server front/public",
    "front-watch": "npm run front-build && parallelshell \"npm run front-watch-typescript\" \"npm run front-watch-browserify\" \"npm run front-serve\" ",
    "front-uglify": "uglify -s front/public/app.js -o front/public/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.6.3",
    "browserify": "^11.2.0",
    "http-server": "^0.8.5",
    "parallelshell": "^2.0.0",
    "react": "^0.14.0",
    "react-bootstrap": "^0.27.1",
    "react-dom": "^0.14.0",
    "rimraf": "^2.4.3",
    "tsd": "^0.6.5",
    "typescript": "^1.6.2",
    "uglify": "^0.1.5",
    "watchify": "^3.4.0"
  },
  "dependencies": {
    "glob": "^5.0.15",
    "musicmetadata": "^2.0.2",
    "uuid": "^2.0.1"
  }
}
